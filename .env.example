# PostgreSQL — alohida baza va alohida foydalanuvchi
DATABASE_URL="postgresql://hisob:PAROL@127.0.0.1:5432/hisob?schema=public"

# Faqat lokal `prisma migrate dev` uchun. Production'da BO'LMASIN
# (`migrate deploy` shadow bazani ishlatmaydi). HECH QACHON asosiy baza bilan bir xil qilmang —
# shadow baza har safar tozalab tashlanadi.
SHADOW_DATABASE_URL=""
